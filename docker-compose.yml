version: '3.8'

services:
  kidstunes:
    build: .
    volumes:
      - ./config.yaml:/app/config.yaml
      - /path/to/kids-music:/app/kids-music  # Adjust path
      - ./kidstunes.db:/app/kidstunes.db
    restart: unless-stopped
